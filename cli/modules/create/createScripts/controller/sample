let { BaseController } = require('web-wombat');

class {{className}} extends BaseController {
	{{methodName}}(request, response) {
		response.end("The {{className}} controller is working!");
	}
}

module.exports = {{className}};
